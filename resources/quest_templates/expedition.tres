[gd_resource type="Resource" script_class="QuestTemplate" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/data/quest_template.gd" id="1_tmpl"]

[resource]
script = ExtResource("1_tmpl")
rhythm = Array[int]([0, 1, 0, 0, 2])
length_range = Vector2i(8, 9)
difficulty_mult = 1.3
gold_formula_mult = 1.15
blurb_templates = PackedStringArray("A long push into the wood's heart - go prepared.", "Few walk this far and fewer walk back with much to show for it.")
