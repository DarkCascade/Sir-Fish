[gd_scene load_steps=3 format=3]

[ext_resource type="Script" path="res://scripts/battle/shop_building.gd" id="1_shop"]
[ext_resource type="PackedScene" uid="uid://dik8qyahy2yd1" path="res://assets/meshes/shop_building.glb" id="2_model"]

; [refinement-pass-3] The shop is a Meshy model now (assets/meshes/shop_building
; .glb - purple plaster hut, crimson shingle roof, a hanging "SHOP" sign on a
; wrought bracket), replacing the BoxMesh + PrismMesh blockout and its four
; cel/outline ShaderMaterials. It carries its own baked material, the same way
; the bioluminescent-forest env props do. It faces +Z (door and sign toward the
; battle camera); the sign juts along -X. WindowGlow keeps a warm point light
; just inside the front window. shop_building.gd only animates the whole node.

[node name="ShopBuilding" type="Node3D"]
script = ExtResource("1_shop")

[node name="Model" parent="." instance=ExtResource("2_model")]

[node name="WindowGlow" type="OmniLight3D" parent="."]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, -0.55, 1.05, 1.15)
light_color = Color(0.831373, 0.658824, 0.262745, 1)
light_energy = 2.0
omni_range = 2.5
